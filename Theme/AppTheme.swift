import SwiftUI

enum AppTheme {
    static let background = Color("Background")
    static let foreground = Color("Foreground")
    static let border = Color("Border")
    static let input = Color("Input")
    static let brandPrimary = Color("BrandPrimary")
    static let brandPrimaryForeground = Color("BrandPrimaryForeground")
    static let brandSecondary = Color("BrandSecondary")
    static let brandSecondaryForeground = Color("BrandSecondaryForeground")
    static let muted = Color("Muted")
    static let mutedForeground = Color("MutedForeground")
    static let card = Color("Card")
    static let surface = Color("Surface")
}
